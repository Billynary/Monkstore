# Secrets
.env
.env.*
!.env.example
*.pem
*.key
!*.pub
secrets/
id_rsa

# Dependencies
node_modules/

# Build output
apps/**/dist/
dist/
build/
coverage/
*.tsbuildinfo

# Runtime data
data/
*.db
*.sqlite
*.sqlite3

# Working notes (kept local, never shipped).
# docs/solutions.md is the sealed hidden-vulnerability answer key and must
# never end up in a repository or an image.
docs/

# Logs
logs/
*.log
npm-debug.log*

# Editors and OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local compose overrides only. docker-compose.yml, deploy/ and .dockerignore
# are TRACKED on purpose: every host (including the homelab LXC) deploys by
# cloning this repository and running `docker compose up -d --build`.
docker-compose.override.yml

# Assistant guidance and machine-local settings (kept local, never shipped).
# .claude/skills/ stays tracked: those are shared project content.
CLAUDE.md
.claude/settings.local.json
.claude/settings.json
